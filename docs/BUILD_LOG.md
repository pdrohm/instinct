# Build Log — evidence-driven implementation record

> One entry per loop. Format: task → hypotheses → change → checks → evidence → remaining.

## 2026-07-02 — Slice 1: Wolf + Stamina (PROTOTYPE_SPEC build order step 1)

**Environment fact:** UE 5.6 not installed on the dev machine at authoring time; Xcode 26.2 present.
All code authored as text (C++ + editor Python); compile/play verification deferred to first
editor launch. Deviation from "Blueprint-first" logged below.

### Deviation D-S1-1: C++-first instead of Blueprint-first
Blueprints/maps are binary assets the agent tooling cannot author. C++ was "already necessary"
(architecture constraint 4). Mitigations: all tuning in `UAnimalConfig` (editor-editable, no
recompile); grey-box map generated by `Scripts/build_greybox_map.py`; zero binary assets required.

### Loop 1 — Scaffold (infrastructure)
- `FirstLife.uproject` (UE 5.6, EnhancedInput + PythonScriptPlugin), Target/Build.cs, module,
  `Config/*.ini` (default map `/Game/Maps/GreyBox`, default GameMode, Enhanced Input classes), `.gitignore`.
- Checks: none available (no engine). Evidence: files exist.

### Loop 2 — Agent body + stamina (H2, H3, H7)
- `UAnimalConfig` (PrimaryDataAsset): WalkSpeed 300, SprintSpeed 750, MaxStamina 100,
  drain 20/s (~5s burst), walk regen 8/s, rest regen 25/s, exhaustion recovery at 30%.
- `UStaminaComponent`: drain while sprint-moving, regen while moving, faster regen resting;
  full burn-out locks sprint until 30% recovery — this is the greed-punishing rhythm.
- `AAnimalCharacter`: generic agent (wolf = config), grey-box body from engine cubes,
  third-person camera, sprint dies mid-stride at exhaustion. Config soft-loads `DA_Wolf`,
  falls back to class defaults.
- Checks: static review only. Evidence: pending first compile.

### Loop 3 — Possession seam (H7)
- `AAnimalAIController`: minimal wander brain — the minimum that makes the AI↔player handoff
  visible (scope-justified: spec calls the seam non-negotiable).
- `AWolfPlayerController`: Enhanced Input built in code (WASD/mouse/Shift/P); P releases the
  body to its AI (player spectates, camera stays on the wolf), P reclaims it.

### Loop 4 — Playable frame (H1, H2, H3)
- `AFirstLifeGameMode` (pawn/controller/HUD wiring), `AFirstLifeHUD` (canvas stamina bar:
  white → amber below 30% → red while exhausted; "AI has the body" hint while spectating).
- `Scripts/build_greybox_map.py`: GreyBox map (100m×100m floor, boundary walls, 3 rocks + a
  rise as LOS breakers, PlayerStart, movable sun/skylight/atmosphere) + `DA_Wolf` asset. Idempotent.

### Loop 5 — Code review (cpp-reviewer agent)
- Verdict: Warning — zero CRITICAL. Possession flow, GC-rooting of runtime input objects,
  and AutoPossessAI semantics confirmed correct.
- Fixed: HIGH tick-order race (StaminaComponent is now tickless; character calls
  `Update(Activity, DeltaTime)` synchronously); explicit `InputActionValue.h` include;
  defensive Cast for the input component; `ReleasedAnimal` reset on reclaim.
- Deferred on purpose: authority guard on the possession toggle (TODO comment left in
  code; only matters at a networked milestone, which is explicitly not a goal).

### Status vs. Definition of Done
- Wolf moves/sprints in grey-box map: **authored, unverified** (needs UE 5.6 install).
- Stamina drains sprinting / regens walking/resting: **authored, unverified**.
- Movement rhythm feel: **untested — requires hands-on play** (this is H2's actual test).
- No extra systems added: **verified by inspection** (no herd/prey/hunger/scent/menus/save/net).

### Remaining
1. Install UE 5.6 → open project → build module (first real check).
2. Run `Scripts/build_greybox_map.py` → Play → verify DoD.
3. Tune `DA_Wolf` until sprint rhythm feels right (budget real time here — spec calls this the fun).
4. Then, and only then: slice 2 (living herd, H5).
